-- V12__insert_topic_arrays.sql
-- Тема “Массивы в Java”
INSERT INTO topic (title, content, type, video_url) VALUES
(
  'Массивы в Java',
  '<p>Массивы в Java — это фундаментальная структура данных, позволяющая хранить упорядоченный набор элементов одного типа. Каждый элемент в массиве доступен по индексу, начиная с нуля, а сама структура представляет собой непрерывный блок в памяти, что обеспечивает очень быстрый доступ и эффективную работу с данными.</p>

  <p>При создании массива его размер фиксируется и не может быть изменён во время выполнения программы. Попытка обратиться по индексу, выходящему за границы <code>0…array.length–1</code>, приведёт к исключению <code>ArrayIndexOutOfBoundsException</code>. Поэтому при любых операциях с массивом важно проверять длину и не выходить за её пределы.</p>

  <p>Массивы могут быть:</p>
  <ul>
    <li><strong>Одномерными</strong> — простой список элементов одинакового типа.</li>
    <li><strong>Многомерными</strong> — массивы массивов, чаще всего двумерными, используемыми для работы с таблицами и матрицами.</li>
  </ul>

  <p>Одним из ключевых приёмов работы с массивами является их перебор (итерация). Для этого удобно использовать:</p>
  <ul>
    <li><code>for</code> с явным индексом — когда нужен доступ к номеру элемента;</li>
    <li><code>for-each</code> — когда важно просто получить все элементы без индексов.</li>
  </ul>

  <p>Многомерные массивы в Java реализованы как «массивы массивов»: у каждой строки может быть своя длина. Обход таких структур выполняется вложенными циклами, но будьте аккуратны с производительностью при больших размерах.</p>

  <h3>Пример кода</h3>
  <pre><code>int[] numbers = new int[5];              // создание одномерного массива
String[] names = { "Alice", "Bob", "Charlie" };  // инициализация массива строк

// перебор с индексом
for (int i = 0; i < numbers.length; i++) {
    numbers[i] = i * 2;
    System.out.println("numbers[" + i + "] = " + numbers[i]);
}

// for-each
for (String name : names) {
    System.out.println("Hello, " + name);
}

// двумерный массив (3×3)
int[][] matrix = new int[3][3];
for (int i = 0; i < matrix.length; i++) {
    for (int j = 0; j < matrix[i].length; j++) {
        matrix[i][j] = i + j;
        System.out.print(matrix[i][j] + " ");
    }
    System.out.println();
}</code></pre>',
  'theory',
  'https://www.youtube.com/watch?v=H4Hc1vBvzkE'
);
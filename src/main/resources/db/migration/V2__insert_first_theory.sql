INSERT INTO topic (title, content, type, video_url) VALUES
(
  'Переменные и типы данных в Java',
  '<p>В Java переменные служат именованным хранилищем данных и позволяют программе работать с информацией, изменять её и передавать между методами. Каждая переменная обладает именем, типом и областью видимости, которые определяют её жизненный цикл и возможности взаимодействия в коде.</p>
  
  <p><strong>Область видимости</strong> (scope) переменной может быть локальной (внутри метода или блока) или глобальной (поле класса). При объявлении важно выбирать информативные имена и соблюдать конвенции Java (camelCase).</p>
  
  <p><strong>Инициализация</strong> переменных — обязательный шаг перед использованием, иначе при компиляции будет ошибка. Ссылочные переменные по умолчанию инициализируются значением <code>null</code>, а примитивные — стандартными значениями (0, false, &#039;\u0000&#039;).</p>
  
  <h3>Примитивные типы данных</h3>
  <ul>
    <li><code>byte</code> (8 бит) — маленькие целые числа.</li>
    <li><code>short</code> (16 бит) — целые числа среднего диапазона.</li>
    <li><code>int</code> (32 бита) — наиболее часто используемый тип для целых чисел.</li>
    <li><code>long</code> (64 бита) — большие целые числа.</li>
    <li><code>float</code> (32 бита) и <code>double</code> (64 бита) — числа с плавающей точкой.</li>
    <li><code>boolean</code> — логический тип (<code>true</code>/<code>false</code>).</li>
    <li><code>char</code> — одиночный символ UTF-16.</li>
  </ul>
  
  <h3>Ссылочные типы данных</h3>
  <p>Объекты классов (например, <code>String</code>, массивы или пользовательские классы) хранятся по ссылке. При присвоении одной ссылочной переменной другой обе указывают на один объект. Избегайте <code>null</code>-указателей, проверяйте переменные перед использованием.</p>
  
  <h3>Пример кода</h3>
  <pre><code>int age = 25;
String name = &quot;Alice&quot;;
boolean isActive = true;
// объявление массива
int[] numbers = new int[]{1, 2, 3};
  </code></pre>',
  'theory',
  'https://www.youtube.com/watch?v=DIakL9uk1nc'
);
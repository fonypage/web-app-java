INSERT INTO topic (title, content, type, video_url) VALUES
(
  'Операторы в Java',
  '<p>Операторы в Java выполняют операции над данными: арифметические вычисления, сравнение, логические комбинации и побитовые сдвиги. Правильное понимание работы операторов и их приоритета позволяет писать выражения без лишних скобок и ошибок.</p>
  
  <p><strong>Приоритет операторов</strong> определяет порядок вычисления в сложных выражениях. Например, <code>*</code> и <code>/</code> выполняются раньше <code>+</code> и <code>-</code>. При равном приоритете действует <strong>ассоциативность</strong> (слева направо).</p>
  
  <p>Используйте скобки для явного указания порядка расчёта и улучшения читаемости кода.</p>
  
  <h3>Основные группы операторов</h3>
  <ul>
    <li><code>Арифметические</code>: <code>+ - * / %</code> — сложение, вычитание, умножение, деление, остаток.</li>
    <li><code>Операторы сравнения</code>: <code>== != &lt; &gt; &lt;= &gt;=</code> — проверка отношений.</li>
    <li><code>Логические</code>: <code>&amp;&amp; || !</code> — объединение условий.</li>
    <li><code>Побитовые</code>: <code>&amp; | ^ ~ &lt;&lt; &gt;&gt;</code> — работа с битами чисел.</li>
    <li><code>Присваивания</code>: <code>=, +=, -=, *=, /=</code> — установка или обновление значения переменной.</li>
  </ul>
  
  <h3>Советы и подводные камни</h3>
  <ul>
    <li>Не путайте <code>==</code> и <code>equals()</code> при сравнении объектов.</li>
    <li>Оператор <code>%</code> может давать отрицательный результат при работе с отрицательными числами.</li>
    <li>Побитовые операции полезны в оптимизациях, но снижают читаемость — используйте осознанно.</li>
  </ul>
  
  <h3>Пример кода</h3>
  <pre><code>int a = 10, b = 3;
int result = a * (b + 2) - b % a;
boolean check = (a &gt; b) &amp;&amp; (b != 0);
  </code></pre>',
  'theory',
  'https://www.youtube.com/watch?v=ll_G3g1t2Z8'
);